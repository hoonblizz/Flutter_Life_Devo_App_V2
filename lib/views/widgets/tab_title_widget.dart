import 'package:flutter/material.dart';
import 'package:flutter_life_devo_app_v2/theme/app_texts.dart';

Padding tabsTitle(title) {
  return Padding(
      padding: const EdgeInsets.all(10),
      child: Text(title, style: tabsTitleFont));
}
